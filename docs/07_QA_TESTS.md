# QA test commands

Run after any routing or UI behaviour change.

```bash
npm run test
npx playwright test e2e/flow.spec.ts
npx playwright test e2e/catalogue.spec.ts
npm run build
```

Full checklists and manual steps: **QA.md** (repo root).
